apiVersion: v1
data:
  group.txt: 'users_rw: check, david, john, nishant

    people_rw: chinta, keys, mouse

    common_rw: prasad, satya'
kind: ConfigMap
metadata:
  name: trino-configmap
  namespace: default
